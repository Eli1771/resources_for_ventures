<h1>Assignment for <%= @assignment.user.name %></h1>
<h3><%= @assignment.viewed ? 'Viewed' : 'Not Viewed' %></h3>
<h3><%= link_to @assignment.material.name, material_path(@assignment.material_id) %></h3>

<%= link_to 'BACK TO YOUR USER PAGE', @user.correct_user_path %>
