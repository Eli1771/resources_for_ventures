<h1>All Assignments</h1>

<ul>
  <% @assignments.each do |assignment| %>
    <li><%= link_to "#{assignment.user.name} - #{assignment.material.name}", assignment_path(assignment) %></li>
  <% end %>
</ul>
